[build-system]
requires = ["maturin>=1.4,<2.0"]
build-backend = "maturin"

[project]
name = "xlsxturbo"
version = "0.21.0"
description = "High-performance Excel writer with automatic type detection (pandas, polars, CSV)"
readme = "README.md"
license = { text = "MIT" }
requires-python = ">=3.9"
keywords = ["excel", "xlsx", "csv", "converter", "performance", "rust"]
classifiers = [
    "Development Status :: 5 - Production/Stable",
    "Intended Audience :: Developers",
    "Intended Audience :: Science/Research",
    "License :: OSI Approved :: MIT License",
    "Operating System :: OS Independent",
    "Programming Language :: Python :: 3",
    "Programming Language :: Python :: 3.9",
    "Programming Language :: Python :: 3.10",
    "Programming Language :: Python :: 3.11",
    "Programming Language :: Python :: 3.12",
    "Programming Language :: Python :: 3.13",
    "Programming Language :: Python :: 3.14",
    "Programming Language :: Rust",
    "Topic :: Office/Business :: Financial :: Spreadsheet",
    "Topic :: Scientific/Engineering",
    "Topic :: Software Development :: Libraries :: Python Modules",
]

[project.urls]
Homepage = "https://github.com/tstone-1/xlsxturbo"
Repository = "https://github.com/tstone-1/xlsxturbo"
Issues = "https://github.com/tstone-1/xlsxturbo/issues"
Changelog = "https://github.com/tstone-1/xlsxturbo/blob/main/CHANGELOG.md"
Roadmap = "https://github.com/tstone-1/xlsxturbo/blob/main/ROADMAP.md"

[project.optional-dependencies]
dev = [
    "pytest",
    "pandas",
    "polars",
    "openpyxl",
    "xlsxwriter>=3.2.9",
    "pyarrow>=21.0.0",
    "maturin>=1.13.3",
    "ruff>=0.14.0",
    "bandit>=1.8.0",
    "pyright>=1.1.390",
    "pandas-stubs",
    # tests/test_docs_site.py parses mkdocs.yml. PyYAML is already present as a
    # bandit transitive, but a test must not depend on another tool's dependency
    # tree -- that breaks silently the day bandit drops it.
    "pyyaml",
    "types-PyYAML",
    # scripts/coverage_report.py measures the pure-Python layer. The Rust half
    # needs `rustup component add llvm-tools-preview`, which pip cannot supply.
    "coverage[toml]>=7",
]

[tool.maturin]
features = ["pyo3/extension-module"]
python-source = "python"
module-name = "xlsxturbo"

[tool.ruff]
target-version = "py39"
line-length = 120
extend-exclude = ["python/xlsxturbo/xlsxturbo.pyd"]

[tool.ruff.lint]
select = [
    "E",    # pycodestyle errors
    "W",    # pycodestyle warnings
    "F",    # pyflakes
    "I",    # isort
    "N",    # pep8-naming
    "D",    # pydocstyle
    "UP",   # pyupgrade
    "ANN",  # flake8-annotations
    "B",    # flake8-bugbear
    "A",    # flake8-builtins
    "C4",   # flake8-comprehensions
    "PIE",  # flake8-pie
    "PT",   # flake8-pytest-style
    "RET",  # flake8-return
    "SIM",  # flake8-simplify
    "TID",  # flake8-tidy-imports
    "PTH",  # flake8-use-pathlib
    "RUF",  # ruff-specific
    "S",    # flake8-bandit
]
ignore = [
    "D203",   # one-blank-line-before-class (conflicts with D211)
    "D213",   # multi-line-summary-second-line (conflicts with D212)
    "ANN401", # allow explicit typing.Any where a precise type is impractical
]

[tool.ruff.lint.pyupgrade]
# Do NOT rewrite `Union[X, Y]` to `X | Y` (UP007/UP045) in `python/xlsxturbo/types.py`.
#
# `requires-python` is `>=3.9`, where PEP 604 unions are a syntax the runtime cannot
# evaluate. `from __future__ import annotations` makes the module *import* fine on 3.9 --
# annotations are never evaluated -- so ruff reasonably concludes the rewrite is safe.
# It is not: `typing.get_type_hints()` evaluates them, and anything building a schema
# from these shapes (pydantic, FastAPI, attrs) does exactly that. It raises on 3.9.
#
# `tests/test_types_module.py::test_no_annotation_uses_a_pep_604_union` is the guard;
# this setting is what stops `ruff check --fix` reintroducing the problem in one keystroke.
keep-runtime-typing = true

[tool.ruff.lint.pydocstyle]
convention = "google"

[tool.ruff.lint.isort]
combine-as-imports = true

[tool.ruff.lint.flake8-annotations]
mypy-init-return = true

[tool.ruff.lint.per-file-ignores]
# Tests: assert is the point; helper builtins shadowing is acceptable.
"tests/**" = ["S101"]
# Benchmarks are throwaway dev scripts: subprocess, print, and non-crypto
# random data generation are all intentional.
"benchmarks/**" = ["S404", "S603", "S607", "S311", "T201"]

[tool.pyright]
include = ["python", "tests", "benchmarks", "scripts"]
exclude = ["**/__pycache__", "**/.venv", "target"]
venvPath = "."
venv = ".venv"
pythonVersion = "3.9"
# Default mode for the project (tests + benchmarks). The shipped library
# (python/xlsxturbo) is raised to "strict" via the `strict` path list below.
typeCheckingMode = "standard"
reportMissingModuleSource = false
strict = ["python/xlsxturbo"]

# Bandit scans the shipped package only. Tests and benchmarks are dev-only
# scripts (asserts and non-crypto random data generation are expected there).
[tool.bandit]
exclude_dirs = ["tests", "benchmarks", "target", ".venv"]
